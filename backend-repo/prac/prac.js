// new changes
// userAV = user.avatar.slice(-1, -11);

// new changes
// userFound.avatar = userFound.avatar.slice(-1, -11);
// console.log("userAV", userFound.avatar);
// userFound.avatar;

// new changes
// const imagesDir = require.context("../images", true);
// const currentPath = "." + loginUser.avatar.slice(-12);
// let avatarPath = imagesDir(`${currentPath}`).default;

// userFound.avatar = `${req.protocol}://${req.get("host")}${
//   userFound.avatar
// }`;
